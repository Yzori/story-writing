---
name: No "prose" terminology
description: Never use the word "prose" in user-facing text — use "novel" instead. User finds "prose" pretentious/outdated.
type: feedback
---

Don't use "prose" anywhere user-facing. The format is called "novel" (as it is on the create page). Internal component names like ProseEditor are fine but any UI strings, labels, or descriptions should say "novel."

**Why:** User explicitly said "I really dont want to hear the word prose anymore - no one uses that in modern context."

**How to apply:** All new and existing UI text, tooltips, labels, stubs, and format references should use "novel" not "prose." This applies to editor names, reading views, format selectors, etc.
