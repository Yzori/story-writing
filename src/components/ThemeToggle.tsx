// The old standalone toggle here flipped local state without ever calling
// setTheme() — a decorative dud. Everything now shares the real lamp switch.
export { default } from "@/components/editor/ThemeToggle";
